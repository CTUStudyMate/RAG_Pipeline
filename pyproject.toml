[project]
name = "rag_experiment"
version = "0.1.0"
description = "RAG experimentation pipeline"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"